# F01 — Postgres schema + db pool + migrations + query helpers (users, items, tags, item_tags, collections, collection_items, srs_state)

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 PIMB

Postgres schema + db pool + migrations + query helpers (users, items, tags, item_tags, collections, collection_items, srs_state)